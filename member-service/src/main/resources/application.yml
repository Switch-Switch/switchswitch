server:
  port: 8083
spring:
  application:
    name: member-service
  profiles:
    active: local
  config:
    import:
      - configserver:http://localhost:8888
      - application-secret.yml
  datasource:
    url: ${db.url}
    username: ${db.username}
    password: ${db.password}
