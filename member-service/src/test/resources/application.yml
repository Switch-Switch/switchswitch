spring:
  application:
    name: member-service
  config:
    import:
      - application-secret.properties

jwt:
  expired:
    access-token: 300000 # 5 minute
    refresh-token: 31536000000 # 1 year